# 🕸️ Graph Code Viewer

**Graph Code Viewer** — это мощный инструмент для визуализации архитектуры и структуры кода на Python и C++. Приложение строит интерактивный граф зависимостей, показывая файлы, классы, функции и переменные, а также связи между ними (вызовы, использование).

Это идеальный помощник для рефакторинга, изучения чужого кода и понимания сложных взаимосвязей в проекте.

---

## ✨ Основные возможности

### 🔍 Анализ и Визуализация
*   **Мультиязычность:** Поддержка проектов на **Python** (`.py`) и **C++** (`.cpp`, `.h`, `.hpp`, `.c`).
*   **Иерархия:** Отображение вложенности: `Файл` → `Класс` → `Метод/Функция`.
*   **Типы связей:**
    *   Сплошные линии: Вызовы функций внутри одного файла.
    *   Пунктирные линии: Вызовы между разными файлами.
    *   Тонкие точки: Использование глобальных переменных.
*   **Подсветка кода:** При клике на узел или стрелку слева отображается исходный код элемента с подсветкой синтаксиса (тема VS Code Dark). Также отображаются комментарии перед функцией.

### 📊 Метрики и Аналитика
При клике на функцию в инфо-панели отображаются метрики:
*   **LOC (Lines of Code):** Количество строк кода. Помогает оценить объем функции.
*   **Complexity (Сложность):** Эвристическая оценка сложности (считает ветвления `if`, `for`, `while`, `switch`).
    *   **Зеленый:** Простой код.
    *   **Красный:** Сложный код (>10).
    *   **Визуализация:** Ноды с высокой сложностью автоматически получают **жирную красную обводку** на графе.

### 🎮 Интерактивность
*   **Умное выделение:**
    *   Клик по ноде подсвечивает её соседей (кто вызывает её и кого вызывает она).
    *   Остальной граф затемняется, чтобы не отвлекать внимание.
*   **Поиск пути:** Выберите две ноды (последовательно), и приложение подсветит **кратчайший путь** вызовов между ними желтым цветом.
*   **История навигации:** Кнопки `←` и `→` позволяют перемещаться по истории выбранных элементов.

### ⚙️ Гибкая настройка (Settings)
*   **Физика графа:** Ползунки для управления силой отталкивания узлов (Content Spread) и длиной связей (Edge Length).
*   **Внешний вид:** Настройка отступов внутри "коробочек" (Box Padding) и расстояния между ними (Box Spacing).
*   **Прозрачность:** Регулировка степени затемнения неактивных элементов (Dimmed Opacity).
*   **Фильтры:** Возможность скрыть/показать переменные, дандер-методы (напр. `__init__`) или классы.

---

## 🚀 Установка и Запуск

### Предварительные требования
У вас должен быть установлен **Python 3.8+**.

### 1. Загрузка проекта

В поле **"File/Folder Path..."** введите полный путь к папке с кодом или к конкретному файлу.

> **Совет:** Можно просто скопировать путь из проводника Windows (**"Копировать как путь"**), кавычки удалятся автоматически.

Нажмите **Enter** или кнопку **Load**. Граф построится автоматически.

### 2. Навигация по графу

- **Зум и панорамирование:** Используйте колесико мыши для масштабирования и перетаскивание (ЛКМ) для перемещения по холсту.
- **Скрытие панели:** Нажмите на стрелочку **‹** на разделительной линии, чтобы скрыть левую панель и развернуть граф на весь экран.
- **Поиск:** Введите имя функции или часть кода в поле **Search**. Кликните по результату, чтобы найти и выделить ноду на графе.

### 3. Работа с элементами

- **Клик по функции (кружок):**
  - Слева покажется код функции и метрики (**LOC**, **Complexity**).
  - Граф перейдёт в режим фокуса: яркими останутся только выбранная функция и те, с которыми она связана.

- **Клик по стрелке (связь):**
  - Слева покажется конкретная строка кода (**snippet**), где происходит этот вызов.

- **Поиск пути:**
  - Кликните на функцию **A**, затем на функцию **B**. Если между ними есть путь вызовов, он подсветится жирной жёлтой линией.

- **Сброс:**
  - Кликните в любое пустое место на графе, чтобы сбросить выделение и вернуть обычный вид.

### 4. Вкладка Settings (Настройки)

Если граф выглядит слишком запутанным или «слипшимся»:

- **Content Spread:** Увеличьте значение, чтобы «раздуть» узлы внутри файлов друг от друга.
- **Edge Length:** Увеличьте, чтобы сделать связи длиннее и граф более размашистым.
- **Box Padding:** Увеличьте, чтобы добавить больше воздуха внутри рамок файлов.
- **Dimmed Opacity:** Настройте, насколько сильно должны исчезать невыделенные элементы (`0` — полностью исчезают, `0.5` — полупрозрачные).
- **Filter Types:** Уберите галочки (например, **"Variables"**), чтобы скрыть лишние элементы и упростить граф.

---

### 🎨 Настройка темы (`config.json`)

Вы можете полностью изменить цвета интерфейса без изменения кода. Создайте файл `config.json` в папке с программой:

```json
{
  "theme": {
    "background_gradient": "linear-gradient(135deg, #1e1e1e 0%, #000000 100%)",
    "sidebar_bg": "#1e1e1e",
    "sidebar_border": "#444",
    "text_main": "#ffffff",
    "text_muted": "#aaaaaa",
    "input_bg": "#2d2d2d",
    "input_border": "#555",
    "accent_color": "#FFD700",
    "edge_color": "#555555",
    "node_bg_default": "#333333",
    "code_bg": "#1e1e1e",
    "scrollbar_thumb": "#555",
    "scrollbar_track": "#1e1e1e"
  }
}

```
### 🛠 Технические детали

- **Python Parsing:** Использует встроенный модуль `ast` (100 % точность, поддержка вложенных классов и функций).  
- **C++ Parsing:** Основан на регулярных выражениях (поддерживает классы, пространства имён, шаблоны, но может пропускать сложные макросы).  
- **Frontend:** Построен на [Dash](https://dash.plotly.com/) (на базе React.js).  
- **Graph Engine:** Используется [Cytoscape.js](https://js.cytoscape.org/) с алгоритмом **COSE** (*Compound Spring Embedder*), специально разработанным для визуализации графов с вложенными группами.

---

### ❓ Частые вопросы

**В:** Почему я не могу изменить размер коробочки мышкой?  
**О:** Размер контейнеров («коробочек») рассчитывается автоматически физическим движком графа на основе их содержимого. Чтобы увеличить расстояние между элементами, используйте ползунки в разделе **Settings → Content Spread** или **Box Padding**.

**В:** Что означает красная обводка у кружка?  
**О:** Красная обводка указывает на высокую цикломатическую сложность функции (>10). Такие функции могут быть трудны для понимания и тестирования — рекомендуется рассмотреть возможность рефакторинга.

**В:** Почему не видна связь между Python и C++?  
**О:** Приложение выполняет статический анализ каждого языка **независимо** и строит вызовы только **внутри одного языка**. Кросс-языковые взаимодействия (например, через `pybind11`, `ctypes` или встроенные интерпретаторы) не отслеживаются, так как их невозможно надёжно определить без выполнения кода или более глубокого семантического анализа.
